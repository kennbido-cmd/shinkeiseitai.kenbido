# 神経整体健美堂鍼灸接骨院 ホームページ

大阪府堺市美原区の整体・鍼灸院「神経整体健美堂鍼灸接骨院」の公式ホームページ。
院長ひとりで運営する完全予約制の院。静的HTML/CSS/JSのみ、ビルド不要（`index.html` を直接ブラウザで開けば表示可能）。

## ファイル構成

- `index.html` — ページ本体（ヘッダー / ヒーロー / コンセプト / 特徴 / 当院について / 神経整体とは / 施術の流れ / お悩み別 / 施術メニュー・料金 / アクセス / よくあるご質問 / ご予約 / フッター）
- ナビゲーション（ヘッダー内）: トップ(`#top`) / 当院について(`#about`) / 神経整体とは(`#shinkei-seitai`) / 施術の流れ(`#flow`) / お悩み別(`#concerns`) / 料金(`#menu`) / アクセス(`#access`) / よくあるご質問(`#faq`) / ご予約(`#reserve`)
- `style.css` — スタイル一式（CSS変数でカラー・フォントを管理）
- `script.js` — モバイルメニューの開閉／施術メニューセクションのタブ切り替え（`.menu-tab`クリックで対応する`.menu-panel`のみ表示）／文字サイズ変更ウィジェットの操作

## 文字サイズ変更機能

2026-09-14、「HPの文字サイズを調整できるように設定してほしい」との要望で、閲覧者が自分でページ全体の文字サイズを変更できるウィジェットを追加。

- 実装方式: `style.css`内の全`font-size`宣言（計60箇所）をPythonスクリプトで px → rem（`px/16`）に一括変換し、`html { font-size: 100%; }`を基準点とした。JSが`document.documentElement.style.fontSize`をパーセント指定で書き換えることで、rem単位の全フォントサイズが連動して拡大・縮小する仕組み（`line-height`は元々ほぼ全箇所unitless指定のため、この変更だけで正しく追従する）。
- ウィジェットUI: 画面右下固定の`.font-size-widget`（「文字サイズ」ラベル＋標準／大／特大の3ボタン、`data-font-scale`属性にそれぞれ100/125/150を指定）。`index.html`内、`.mobile-call-btn`の直後・`</body>`直前に配置。デザインは既存ボタン類と統一感を持たせ、`--color-line-strong`の細罫線・角丸2px・ホバーで`--color-primary`反転。現在選択中のボタンは`.active`クラスで`--color-accent`（朱色）反転表示し、どのサイズを選んでいるか一目でわかるようにしている。
  - 当初は－／標準／＋の増減式（6段階）で実装したが、「文字の大きさを標準　大　特大　くらいにしてもらえますか」との要望を受け、意味のわかる3段階の直接選択式に変更した。今後さらに細かい段階が必要になった場合もこの直接選択パターン（ボタンラベル＝サイズ名、クリックで即該当スケールに切替）を踏襲すること。
- 永続化: `localStorage`（キー`seitai-font-scale`）に保存し、再訪問・別ページでも設定を保持。`<head>`内の`style.css`読み込み直後にインラインスクリプトを置き、ページ描画前に保存値を`documentElement`へ同期適用することでFOUC（一瞬デフォルトサイズが見えるちらつき）を防止している。
- モバイル（860px以下）では`.mobile-call-btn`（画面下部固定・電話予約バー）と重ならないよう、既存の`@media (max-width: 860px)`ブロック内で`.font-size-widget`に`bottom: 68px`を指定してクリアランスを確保（ラベルは非表示にしてボタンのみ表示、幅を圧縮）。
- 今後フォントサイズや行間を新規追加する際は、px直書きではなくremで統一すること（この一括変換後の基準に合わせる）。

## デザイン方針

和モダン（Japanese Modern）を採用。

- 見出し・本文ともに明朝体（`--font-heading` / `--font-body` とも同じ明朝フォントスタック）で統一し、和の情緒を強調
- カラーは生成り／和紙色（`--color-bg: #f7f2e6`）、藍色に近い深緑（`--color-primary: #1f3b3d`、コンセプト帯やフッターに使用）、朱色（`--color-accent: #b23a2e`、はんこ・アクセント用）の3トーン構成
- 背景に和紙のような微細なドット模様（`radial-gradient`）を敷き、質感を演出
- ヘッダーロゴは`images/logo.png`（円形・緑と朱の勾玉風デザイン、透過PNG）を使用。見出しラベル・ヒーローのアイキャッチには引き続き朱色の丸（印/はんこのモチーフ）を使用
- ヘッダーナビゲーション: ロゴ画像追加でロゴ幅が広がり、`max-width: 860px`のハンバーガー切り替えのままだと860〜1040px幅でナビ項目が折り返して2行になる不具合があったため、ナビ/ハンバーガーの切り替えだけを独立したメディアクエリに分離（他のグリッド・ヒーロー等のモバイル調整は引き続き860px）。あわせて`.nav a`に`white-space: nowrap`を追加。ブレークポイントは当初`max-width: 1040px`だったが、2026-09-08にサイト全体のフォントサイズを一律12%拡大した際、ロゴ文字が3行に折り返して幅が広がり、1040〜1200px幅あたりでロゴとナビ「トップ」が重なる不具合が再発したため`max-width: 1220px`に引き上げ済み。今後さらにフォントサイズを変更する場合はこのブレークポイントも合わせて要調整。
  - 上記のブレークポイント調整後も、`.logo-text`自体に`flex-wrap: wrap`が設定されていたため、`.header-inner`がflexで窮屈になると（特に大きいフォントサイズ時）ロゴ文字「神経整体」「健美堂」「鍼灸接骨院」が2〜3行に折り返り、`.header-inner`の固定`height: 78px`を超えてはみ出し、スクロール時に開くモバイルナビ（`.nav.open`、`position: fixed; top: 78px`）と重なって見える不具合があった。「左右に余白があるので余白をなくして」「右側に余裕があるのに文字を3段にしないでほしい」という指摘を受けて調査した結果判明したもの。`.logo-text`を`flex-wrap: nowrap`に変更し、`.logo`と`.logo img`に`flex-shrink: 0`を追加して、ロゴ側は常に1行・崩れない幅を確保し、狭くなった場合はナビ側（ハンバーガー化）で吸収するようにして解決済み。
  - ヘッダーの左右余白について「ヘッダー全体（ロゴ・ナビ）を左に寄せたい」との要望があり、`.header-inner`だけ`.container`の`max-width: var(--max-width)`（1120px）＋中央寄せを外し、`max-width: none; padding: 0 32px;`を個別指定した。しかしこれだとロゴが左端に寄る一方でナビが画面右端まで広がってしまい、間に大きな空白ができて「間を空けないようにバランスも整えて」と指摘された。最終的に`.header-inner`は`max-width: 1280px; margin: 0; padding: 0 32px;`（他セクションと同じ`max-width`変数は使わず、ヘッダー専用に少し広めの固定値。中央寄せの`margin: 0 auto`ではなく左寄せの`margin: 0`）に調整し、ロゴは左端付近、ナビはそこから1280px幅の範囲内で右寄せというバランスに落ち着いた。ヘッダー幅を調整する際は「全体を画面幅いっぱいに」ではなく、この「左寄せ＋適度なmax-width」の形を踏襲すること。
- サイト全体のフォントサイズは2026-09-08に一律12%拡大（「文字の大きさを大きくしてほしい」との要望）。`style.css`内の全`font-size`宣言（メディアクエリ内の上書き含む、計59箇所）をPythonスクリプトで一括して`元の値 × 1.12`（0.5px単位に丸め）に置換したもの。今後フォントサイズを個別に調整する際は、この一括変更後の値が基準になっている点に留意（例: `.hero h1`は元38px→42.5px、`h2`は元28px→31.5px）。
- ボタン類（`.btn`＝ご予約・電話で予約する等 / `.menu-tab`＝施術メニューのタブ / `.mobile-call-btn`＝モバイル固定の電話ボタン）には、2026-09-08に「ボタンを全てキラキラするようにしてほしい」との要望で光沢（シャイン）アニメーションを追加済み。各要素に`position: relative; overflow: hidden;`（`.mobile-call-btn`は元々`position: fixed`なので`position`は変更せず`overflow: hidden`のみ追加）を設定し、共通の`::after`疑似要素（斜めの細い帯状`linear-gradient`、暖色の生成り〜金色`rgba(255,244,214, ...)`で「和」の質感に寄せている）を`btn-shine`という`@keyframes`で右方向へ定期的にスイープさせている。アニメーションは1周期3.4秒のうち最初の約0.75秒だけ帯が通過し、残りは画面外で待機するため、控えめな間隔でキラッと光る見た目になる。ボタンの見た目や配置を変える際は、この`::after`用の`overflow: hidden`を消さないよう注意（消すと光沢が要素の外にはみ出して表示される）。
- カード類は罫線グリッド区切り、ボタンはごく小さい角丸（2px）と細い罫線で、和紙・障子を思わせる直線的な余白設計

## 店舗情報

- 店名: 神経整体健美堂鍼灸接骨院（しんけいせいたいけんびどうしんきゅうせっこついん）
- 住所: 大阪府堺市美原区多治井298−10
- 駐車場: 1台あり
- 電話番号: 090-1716-5939
- 営業時間: 11:00〜19:00
- 定休日: 不定休
- 予約方法: 公式LINE / 予約サイト / 電話（3導線を `#reserve` セクションに設置）。同セクションには2026-09-08追加の「薬膳スクール」カード（予約導線ではなく、姉妹事業への外部リンク）も並んでいる
- 運営体制: 院長ひとりによる完全担当制・完全予約制

## 料金メニュー

| メニュー | コース | 時間 | 料金 |
|---|---|---|---|
| 神経整体 | 初診 | 60〜90分 | 11,000円 |
| 神経整体 | 2回目以降（単発） | 45〜60分 | 11,000円 |
| 美容鍼灸 | ライトコース | 15〜30分 | 3,300円 |
| 美容鍼灸 | スタンダードコース | 30〜45分 | 5,500円 |
| 美容鍼灸 | プレミアムコース | 45〜60分 | 9,900円 |
| 水素吸引 | 初回（カニューラ付き） | 30分 | 4,800円 |
| 水素吸引 | 通常コース | 30分 | 3,300円 |
| 鍼灸施術 | — | 20〜30分 | 3,300円 |

## コンセプト・訴求文言（サイトに反映済み）

- 「年齢やジェンダーにとらわれない。あなたに合ったプランで、身体の外からも中からも、健康で元気に。」
- 「整体はボキボキしない、やさしい施術。その日だけで終わらない、健康な身体づくりをサポートします。」
- 「完全予約制のため、待ち時間等はありません。」
- 神経整体の説明: 感覚神経・運動神経・自律神経が身体をコントロールしており、神経を調整して自然な動きを取り戻すことで不調を改善する、という考え方
- 神経整体は問診・検査を丁寧に行ってから施術するため、予約時は時間に余裕を持ってもらうよう案内

## お悩み別セクション（`#concerns`）

6件のカードを表示（`.concern-grid` / `.concern-card`）。

1. 肩こり・首のこり
2. 腰痛
3. 頭痛・めまい
4. 猫背・姿勢の歪み
5. 冷え・むくみ
6. 眠りの質・疲れが取れない

## 施術の流れ（`#flow`）

4ステップを縦一列に表示（`.flow-list` / `.flow-row`）。各行は写真（左）＋STEPラベル・タイトル・説明文（右）の横並び構成、行間は罫線区切り。

1. 問診（`images/flow-mondou.jpg`）
2. 検査（`images/flow-kensa.jpg`）
3. 施術（`images/flow-sejutsu.jpg`）
4. 次回の予約（`images/flow-yoyaku.jpg`）

画像は`.flow-row-image`（`aspect-ratio: 4/3`、`object-fit: cover`）。860px以下では`.flow-row`が1カラムに変わり、画像が上・テキストが下の縦並びになる。
※当初は4カード横並びグリッド（`.flow-grid`）で実装したが、「横置きじゃなくて縦置きにできますか」という要望を受け、写真＋テキストを横並びにした行を縦に積む現在のレイアウトに変更した。

- **写真のズーム表示（2026-09-21）**：4枚の写真（`flow-mondou.jpg`等）はいずれも1400×1050px・4:3比率で、表示ボックス（`.flow-row-image`、`aspect-ratio:4/3`）と比率が一致しているため`object-fit:cover`が実質何もクロップしておらず、部屋全体が縮小表示されるだけで「画像がアップになっていない」と指摘された。`.flow-row-image img`に`transform: scale(1.35)`を追加し、中央（院長と患者）を拡大表示することで寄った印象にした。今後、構図の異なる写真に差し替える場合は、被写体が中央からずれていないか確認し、必要ならこのスケール値を調整すること。
- **STEP01〜04の順次フェードインアニメーション（2026-09-21新規実装）**：「順番に出てくるようにしていたはず」との申し出を受けて調査したが、コード・Git履歴のどこにも実装が見当たらなかったため、新規に追加した。`style.css`で`.flow-row`に初期状態（`opacity:0; transform:translateY(28px);`）と`transition`を設定し、`.is-visible`クラスが付くと表示状態に遷移する（`prefers-reduced-motion: reduce`では即表示）。`script.js`側で`IntersectionObserver`により各`.flow-row`が画面に入ったタイミングを検知し、DOM順のindexに応じて`0.15s`ずつ`transitionDelay`をずらしながら`is-visible`を付与することで、スクロールに合わせて上から順番にフェードインするようにしている（一度表示されたら`unobserve`して以降は監視しない）。今後ステップを増減する場合もこの仕組みは`.flow-row`要素であれば自動的に適用される。

## よくあるご質問（`#faq`）

`<details>`/`<summary>` によるアコーディオン形式（JS不要、`.faq-item`）。7件。

1. 初めてでも施術を受けられますか？
2. 予約は必要ですか？
3. 施術は痛みがありますか？
4. どのくらいの頻度で通えばいいですか？
5. 駐車場はありますか？
6. 予約の変更・キャンセルはどうすればいいですか？
7. 同業者（整体師・セラピスト等）でも施術を受けられますか？

## ご予約セクション（`#reserve`）のボタン配置

`.reserve-grid`は現在4カラムのカード（`.reserve-card`、display:flex; flex-direction:column）で、各カード内の`.btn`は末尾の子要素にすることで、`p`（説明文、`flex-grow:1`）が余白を吸収し4つの`.btn`が横一列（同じ高さ・同じY座標）に揃う設計になっている。
「お電話で予約」カードのみ、当初は`.btn`の後に`.reserve-note`（営業時間の注意書き）を置いていたため、`.btn`が末尾要素でなくなり、他の2つのボタンより上にずれて表示され「ボタンの高さを同じにしてほしい」と指摘された（実際のボタン自体の高さ・フォントサイズは常に同一だったが、位置がずれて見えた）。`.reserve-note`を`.btn`より前（説明文の直後）に移動し、`.btn`を常に各カードの最後の子要素にすることで解決済み。今後このセクションに要素を追加する場合も、`.btn`は各カードの最後に置くこと。
- 2026-09-08、4枚目のカードとして「薬膳スクール」への外部リンクを追加（`.reserve-grid`を`repeat(3,1fr)`→`repeat(4,1fr)`に変更）。リンク先は`~/Desktop/yakuzennkanrishi`（同じ運営者kenbidoさんが運営する「健美堂薬膳スクール」の集客用ページ、その他のプロジェクト）が公開しているClaude Artifact URL: `https://claude.ai/code/artifact/ded94127-5001-48d7-96a0-2a3db55c975c`（ログイン不要・一般公開向け、詳細は`~/Desktop/yakuzennkanrishi/CLAUDE.md`参照）。見出し・ボタン文言は他の3カードと文字数を揃えるため「薬膳スクール」「薬膳スクールへ」と短めにしてある（長いと1080px以下でボタン内が2行に折り返るため）。

**本サイトはVercelに本番デプロイ済み（2026-09-08、現在の正式な公開URL）**：`https://shinkeiseitai-kenbido.vercel.app`（Vercelプロジェクト名`shinkeiseitai-kenbido`、チーム`v-b-scorer`＝予約システム`yoyakukannri`や`kaigo-kintai-app`と同じスコープ）。`npx vercel project add shinkeiseitai-kenbido` → `npx vercel --prod --yes --project shinkeiseitai-kenbido`でデプロイ（`index.html`・`style.css`・`script.js`・`images/`をそのままアップロードする形。ビルド不要な静的サイトのため追加設定なし）。健美堂薬膳スクール側の「整体院サイトへ」ボタンはこのURLにリンクしている。**Googleマップの`<iframe>`埋め込みがそのまま動く**のがVercel版の利点（Claude Artifactにはこの制限がない）。
  - ローカルの`index.html`を変更した際は、`vercel --prod --yes --project shinkeiseitai-kenbido`を再実行すれば同じURLに再デプロイされる（gitコミット・pushは不要、ローカルファイルを直接アップロードする方式）。GitHubリポジトリ（`git@github.com:kennbido-cmd/shinkeiseitai.kenbido.git`）とは現状連携していない別経路のデプロイなので、git側の変更とVercel側の反映は自動連動しない点に注意。
  - **再デプロイ忘れが薬膳サイト経由で発覚した事例（2026-09-21）**：この日、上記の「神経整体とは」「当院について」「施術の流れ」の一連の修正をローカルで行った後、Vercelへの再デプロイをしないまま作業を進めてしまい、「薬膳サイトから整体院サイトに行くと前の状態のHPになってしまっている」とユーザーから指摘があった（健美堂薬膳スクール側の「整体院サイトへ」ボタンはこのVercel URLにリンクしているため、ローカルの変更がそのままでは反映されない）。`npx vercel --prod --yes --project shinkeiseitai-kenbido`で再デプロイし解決。**ローカルファイルを編集しただけでは公開URLには一切反映されないので、複数箇所を修正した後は必ずこのコマンドで本番デプロイし直すこと。**なお本番デプロイはClaude Codeの自動モードでは確認プロンプトが入る操作のため、実行前にユーザーの了承を得ること。
  - 経緯：健美堂薬膳スクール側のサイトに「整体院サイトへ」という逆リンクカードを追加する際、当初ローカルのfile://パスを指定→公開Artifactからfile://リンクが機能せず断念→本サイトをClaude Artifactとして公開し直し→Artifact版はCSPの制限でGoogleマップの`<iframe>`埋め込みが使えず静的地図画像で代替→「本物の埋め込み地図を使いたい」との要望を受け、最終的にVercelへの本番デプロイに切り替えた。

**Claude Artifact版も別途公開済み（Vercelデプロイ以前の経緯、現在は使っていない）**：
  - 公開URL: `https://claude.ai/code/artifact/1aabde5a-6d57-420e-8084-112fb30c4823`（ログイン不要・一般公開向け）
  - publish用に、`index.html`・`style.css`・`script.js`を1ファイルに統合し（`<style>`/`<script>`にインライン化）、`images/`内の実際に使われている12枚の写真（ロゴ含む、計約3.9MB）をすべてbase64データURIとして埋め込んだバージョンを作成して公開。**未使用の`images/shisetsu-naikan.jpg`は埋め込み対象外**。
  - Googleマップの埋め込み`<iframe>`はArtifactのCSPで表示できないため、当初「Googleマップで開く」という文字だけのリンクに置き換えたが、「この感じ（実際の地図画像）に開けないですか」と指摘を受けた。そこでOSM（OpenStreetMap）のタイル画像（`https://tile.openstreetmap.org/`、APIキー不要）を3x3枚取得してPython/Pillowでスティッチし、Pillowで赤いピンを描画・640x400にクロップした静的地図画像を生成（`images/access-map.jpg`、約48KB）してbase64埋め込みし、クリックで実際のGoogle Maps（`https://www.google.com/maps/search/?api=1&query=...`、正確な住所でジオコーディングされる）を新規タブで開けるようにした。**座標は同社サービスのNominatim（`https://nominatim.openstreetmap.org/search`）で「大阪府堺市美原区多治井」を検索して得た町丁目レベルの中心座標（34.5441940, 135.5668710）であり、298-10番地そのものの厳密な位置ではない**（Nominatimは番地までの住所では検索結果が返らなかったため）。今後より正確な地図画像に差し替えたい場合は、実際の緯度経度を確認した上で同じ手順（タイル取得→スティッチ→ピン描画→base64埋め込み）を踏むこと。
  - **ローカルの`index.html`自体（iframe埋め込みのまま）は変更していない**——ローカルで直接開く分にはインタラクティブなiframeの方がUX上優れているため、publish用バージョンだけこの差分がある。
  - **今後`index.html`・`style.css`・`script.js`のいずれかを変更した場合、この公開Artifactには自動反映されない。変更内容を公開版にも反映したい場合は、上記の統合・base64埋め込み・マップiframe置き換えの手順を踏んだ一時ファイルを作り直し、`Artifact`ツールのpublishアクションで同じURLに再公開すること**（`~/Desktop/yakuzennkanrishi/CLAUDE.md`の「重要：ローカルファイルと公開Artifactは別物」セクションも参照）。
- カード数が4になったことで、1080px以下では2カラム（`@media (max-width:1080px){ .reserve-grid{grid-template-columns:repeat(2,1fr);} }`を新規追加）、860px以下では既存の1カラムに切り替わる。

## 施術メニュー・料金のタブ切替

`#menu` セクションは神経整体／美容鍼灸／水素吸引／鍼灸施術の4タブ構成（`.menu-tab` / `.menu-panel`）。
タブをクリックすると対応するメニューのみ表示され、他は非表示になる。初期表示は「神経整体」。

## 神経整体メニューの注意事項

- 当院は「神経整体Tグループ」の加盟店
- 神経整体を予約する同業者への施術はお断りしている旨をメニュー内に明記済み（`.menu-notice`）

## 表現上の注意点（薬機法）

- 美容鍼灸プレミアムコースは「ヒト幹細胞培養液高濃度60%配合のRICH CELL」を使用する内容だが、当初案の「肌の再生を促進」「肌改善」等は医薬品的な効能を断定する表現に該当しうるため、以下のように修正済み:
  - 「肌の症状を整え」→「お肌の状態を整え」
  - 「新しい細胞を生み出し、肌の再生を促進」→「うるおいに満ちたお肌へ導きます」
  - 「肌改善を目指される方」→「美肌を目指される方」
- 成分名（ヒト幹細胞培養液・RICH CELL）自体の記載は事実表示として残しているが、今後メニュー文言を追加・変更する際も「治る」「改善する」「再生を促進する」等の断定的な効能表現は避け、「整える」「サポートする」「導く」等の表現に留めること。
- 水素吸引メニュー（`#menu-panel-suiso`）の説明文は、院長からいただいた印刷物（水素吸入器の販促ポスター）をもとに作成。ただし元の印刷物には「高血圧」「動脈硬化」「心筋梗塞」「脳卒中」「認知症」「各種ガン」等の疾患名を挙げて「こんな症状に効果的」と断定する記載があり、これは医薬品的な効能効果を標榜する表現として薬機法・景品表示法上のリスクが高く、また実際の疾患治療を水素吸引で代替させかねない内容のため、サイトには反映していない。今後この印刷物をもとに文言を追加・変更する場合も、疾患名を挙げた効果効能の記載は避けること。
  - 一方、活性酸素（ヒドロキシラジカル）と水素が結びついて水になり排出される、という一般的な仕組みの説明は、特定の疾患名を挙げていないため掲載済み（院長の希望により追加）。ただし「身体の不調や老化の一因になる」「体外へ排出される」等の記述は「〜といわれています」「〜とされています」という伝聞・推定のニュアンスを保ち、断定表現にしないこと。
- 最終的な広告表現の適法性は、必要に応じて専門家に確認することが望ましい。

## 院長の資格

- 保有資格: 鍼灸師・柔道整復師・医薬品登録販売者（いずれも医療系国家資格） / 国際薬膳資格
- サイト表示は資格名を個別列挙せず、「医療系国家資格・国際薬膳資格保有」の1バッジにまとめて表示（`#about` セクション `.about-qualification`）
- 本文中には内訳（鍼灸師・柔道整復師・医薬品登録販売者などの医療系国家資格に加え、国際薬膳資格を活かし…）を記載済み

## 画像アセットの取り扱い（GitHub運用向け加工）

`images/` 内の写真はすべて、GitHubリポジトリでの管理・GitHub Pages等での配信を想定し、以下の方針で加工済み。

- 元データはiPhone等で撮影したPNG（1枚あたり2.1〜2.4MB、ファイル名はランダムな16進数のGUID）
- `sips` でJPEG変換・リサイズ（幅1400px程度）・圧縮（quality 80）を行い、1枚あたり290〜380KB程度に軽量化
- ファイル名は内容が分かる英数字（ローマ字・kebab-case寄り）に変更（例: `seitai-sejutsu.jpg`、`shinkyu-mondou.jpg`）。Git上で問題になりやすい空白・日本語・記号を含むファイル名は使用しない
- 追加済みの写真:
  - `images/seitai-sejutsu.jpg` — 神経整体タブ用。院長が施術（背骨まわりの調整）をしている様子
  - `images/shinkyu-mondou.jpg` — 鍼灸施術タブ用。院長がクリップボードを使って問診している様子
  - `images/suiso-kyuin.jpg` — 水素吸引タブ用。実際の院内でMERUSA1000を使用している様子（旧`suiso-kyuin.png`は観葉植物のある別カットだったため差し替え、削除済み）
  - `images/biyoushinkyu.jpg` — 美容鍼灸タブ用。院長が顔に実際にはり治療を行っている様子（旧写真は横向きに座った女性の別カットだったため差し替え）。差し替えに伴い表示パターンも`.menu-side-image`（縦長トリミング）から`.menu-side-image-wide`（横長・全体表示）に変更
  - `images/shinkyu-sejutsu.jpg` — 鍼灸施術タブ用。院長が背中にはり治療を行っている様子
  - `images/about-mondou.jpg` — 「当院について」（`.about-image`）用。院長が問診している様子（元は鍼灸施術タブの写真だったが`shinkyu-sejutsu.jpg`追加に伴い改名・移動）。旧`images/shisetsu-naikan.jpg`（院内の施術スペース写真）と差し替え。`shisetsu-naikan.jpg`ファイル自体は`images/`に残っているが現在どこからも参照されていない

## 「当院について」の写真レイアウト（2026-09-21調整）

`#about`セクションは左カラムに本文（`.about-text`）、右カラムに写真（`.about-image`）を並べる2カラム構成（`.about-inner`、`grid-template-columns: 1.1fr 0.9fr`）。写真は本文より短く、`align-items: start`（初期状態）だと本文の方が長く続き、右カラムの写真の下に大きな空白ができて「バランスを揃えてほしい」と指摘された。試行錯誤の末、以下の形に落ち着いている。

1. まず`align-items: stretch`＋`.about-image{ height:100%; min-height:420px; }`＋`object-fit: cover`で、写真を本文と同じ高さまで引き伸ばして下端を揃えた。
2. 続けて「この画像全て写した状態でこのサイズにできますか」との要望を受け、`object-fit`を`cover`→`contain`に変更（トリミングなしで全体表示）。ただしこれにより、引き伸ばされた縦長のボックス内で画像本来の比率との差分が空白（レターボックス）として見えてしまった。
3. 「この位置でいいので余白分を消してほしい」との指摘を受け、最終的に`align-items: stretch`をやめて`align-items: center`に変更し、`.about-image`側も`height:100%`等の指定を外して`width:100%; height:auto;`の自然なアスペクト比（画像本体は`images/about-mondou.jpg`、4:3）に戻した。これにより写真そのものには余白が生まれず、かつ`.about-inner`が写真ボックスを本文の高さの中で縦方向に中央配置してくれるため、写真が極端に上や下に偏らず、本文の中間あたりに自然に収まるバランスになった。

今後この`.about-image`を調整する際は、「本文の高さに合わせて引き伸ばす（`stretch`+`cover`、トリミングあり）」か「画像本来の比率のまま中央配置する（`center`+`contain`不要、レターボックスなし）」のどちらかを意識し、両者を同時に満たそうとする（=引き伸ばした上でトリミングなし）と必ず内部に余白ができる点に注意すること。
- いずれも既存の`.menu-with-image` / `.menu-side-image-wide`パターン（`aspect-ratio: 5/4`、`object-fit: contain`、横長画像をトリミングせず全体表示）を踏襲
- トップ（ヒーロー）セクションは`.hero`に背景画像を使用（2026-09-08、院長の希望により再追加）。過去に一度「画像なし方針」に戻した経緯があるため、今後変更する際は要注意。
  - 実装の変遷: (1) 最初は`images/seitai-sejutsu.jpg`（神経整体タブと共用）に写真全体グラデーション（`linear-gradient(90deg, ...)`、左が不透明・右が透過）を重ねる方式にしたところ「背景に文字が負けている」と指摘され、グラデーションを強めて左〜中央をほぼ不透明にした。(2) すると今度は「背景が死んでしまっている」（写真がほぼ見えなくなった）と指摘されたため方針転換し、`.hero`自体には写真をそのまま（グラデーションなし）表示、`.hero-inner`（テキスト・ボタンを含むブロック）だけに半透明の背景＋`backdrop-filter: blur(4px)`のすりガラス効果を付ける「パネル方式」にした。(3) `seitai-sejutsu.jpg`は生成りの壁・ベージュのカーテンなど背景パネルと色味が近く「背景を使ってる意味がない」と指摘されたため、色のコントラストが強い`images/suiso-kyuin.jpg`（黒い水素吸入器・青いLED・紺の服＝水素吸引タブと共用）に差し替えた。(4) それでもパネル方式は「最初の背景が薄く写っている感じの方が良かった」と指摘されたため、(1)の全面グラデーション方式に戻しつつ、`.hero-inner`のパネル背景・ぼかしは撤去。現在は`.hero`全体に均一の半透明グラデーション（`linear-gradient(rgba(247,242,230,0.65), rgba(247,242,230,0.65))`をimage手前に重ねる＝実質的に均一のrgba色フィルター）を重ね、写真全体がうっすら透けて見える仕上がりにしている（860px以下は`0.72`でやや濃いめ）。
  - 現状の結論: この種の「背景写真＋テキスト」は、(1)の全面均一半透明オーバーレイ方式が最終的に採用された。今後同様の場面ではこの方式を初期案として踏襲し、必要ならオーバーレイの濃さ（rgba のalpha値）だけを微調整すること。パネル方式（(2)）や強いグラデーション方式は不採用になった経緯があるため、いきなり採用しない。
  - 背景写真を選ぶ際は、パネル色（生成り `#f7f2e6` 系）と被らない、暗色・彩度の高い要素（黒い機材、紺の服など）を含む写真を優先すること（現在は`images/suiso-kyuin.jpg`を使用）。
  - (6) さらに「文字の色をもっとはっきりわかる色か、白枠を入れてほしい」という要望に対し(5)で濃色文字＋白系text-shadowにしていたが、今回は「背景があるところ白文字黒枠に変えてほしい」との指摘を受け、`.hero-eyebrow` / `.hero h1` / `.hero-text` / `.hero-info` / `.hero-info strong`を`color: #ffffff`＋黒系（`rgba(20,18,14,0.92)`）の8方向`text-shadow`（1.5pxオフセット）＋ぼかしグローに変更。あわせて`.hero-info`と`.hero-info strong`に個別で残っていた`color: var(--color-text)`指定（共通ルールを上書きしてしまっていた）を削除済み。ヒーロー内の文字色を今後変える際は、共通セレクタ側だけでなく個別ルール側の`color`指定も確認すること。
  - (7) 「ここに泡がキラキラ浮かんでくる感じにしてほしい」との要望で、水素吸引メニュー（H2/O2の水素発生器）にちなんだ泡アニメーションを追加。`.hero`の直下に`.hero-bubbles`（`position:absolute; inset:0; overflow:hidden; pointer-events:none; z-index:1`）を挿入し、その中に`<span class="bubble">`を13個、それぞれ`left`・`width`/`height`・`animation-duration`・`animation-delay`をインラインstyleで指定してランダムな見た目にしている。`.bubble`は白〜水色の`radial-gradient`＋発光する`box-shadow`の円で、`bubble-rise`（下から上へ`translate`しながらフェードイン・フェードアウト）と`bubble-twinkle`（`filter: brightness`を周期的に上げてキラキラさせる）の2つの`@keyframes`アニメーションを同時適用。`.hero`に`position:relative; overflow:hidden`、`.hero-inner`に`position:relative; z-index:2`を追加し、泡が文字より下・写真より上のレイヤーに収まるようにしている。泡の数や色を調整する際はこのHTML内の`<span class="bubble" style="...">`を増減・編集すること（JSは使っていない、純CSSアニメーション）。
  - (5) 均一オーバーレイ方式に戻したあとも「文字の色をもっとはっきりわかる色か、白枠を入れてほしい」との要望があったため、`.hero-eyebrow` / `.hero h1` / `.hero-text` / `.hero-info` / `.hero-info strong`の文字色を`--color-text-light`から`--color-text`（より濃い色）に変更し、あわせて8方向の`text-shadow`（`rgba(247,242,230,0.95)`で1px、＋`rgba(247,242,230,0.8)`で16pxのぼかし）を重ねて白い縁取り＋淡いグロー効果を付けた。写真背景の上に載る文字には、濃い文字色＋白系text-shadowの縁取りをセットで使うこと。
- `images/logo.png` — ヘッダーロゴ（400x400、透過PNG）。円形の緑/朱ツートンに「神経整体サロン健美堂」の文字と両手のイラストをあしらったデザイン
- `images/evidence.jpg` — 神経整体T-Groupのエビデンス資料（2021日本疲労学会学術大会発表分）。クリックで別タブ拡大表示（`.seitai-evidence-image`）。配置は下記「『神経整体とは』セクションのレイアウト」参照

## 「神経整体とは」セクションのレイアウト（2026-09-21改修）

当初は左カラムに画像2枚（`.seitai-image`＝3神経イラスト／`.seitai-evidence-image`＝エビデンス資料）を縦に並べ、右カラムに見出し・本文・エビデンス文章をまとめる左右2カラム構成（`.seitai-media` / `.seitai-text`）だった。「①説明文→②イラスト画像→③エビデンス説明文→④エビデンス画像の順で縦一列に並べてバランスを整えたい」との要望を受け、`.seitai-media`/`.seitai-text`のラッパーを廃止し、`.seitai-inner`（`max-width: 760px`、中央寄せ）の中に上記4ブロックを直接その順番で並べる単一カラム構成に変更した。

- 画像2枚（`.seitai-image` / `.seitai-evidence-image`）は、当初高さ320px固定＋`object-fit: contain`のレターボックス表示だったが、単一カラム化で列幅（760px）に対して画像が小さく表示され「左右の余白をなくしてほしい」と指摘された。原因は各画像のアスペクト比（イラスト3:2、エビデンス資料4:5）が固定ボックスの比率と合っておらず、`contain`で余白（レターボックス）ができていたこと。`height`固定と`object-fit: contain`を廃止し、`width: 100%; height: auto;`で画像本来の縦横比のまま列幅いっぱいに表示するよう変更（`.seitai-image::before`の装飾枠はそのまま画像の上に重なる形で残している）。モバイル用の固定`height: 220px`指定も同様の理由で削除済み。
- 今後この2枚の画像を差し替える際、新しい画像のアスペクト比によっては表示高さが大きく変わる点に注意（現在は`width:100%; height:auto`で自然な高さになる設計のため、極端に縦長・横長な画像を使うとセクション全体の縦幅に影響する）。

## 未対応・要確認事項（TODO）

- [x] 公式LINEの友だち追加URL（`https://lin.ee/9CxcSTY` を設定済み）
- [x] 予約サイトのURL: 自社開発の予約システム`https://yoyakukannri.vercel.app`に切り替え済み(2026-09-02。旧STORES予約`https://kisitu-kenbido.stores.jp/reserve/kenbido-shinkyusekkotuin`から変更)。予約システム本体のリポジトリは別プロジェクト(`~/Desktop/yoyakukannri`)
- [x] 美容鍼灸の施術風景写真（`images/biyoushinkyu.jpg`、院長が実際にはり治療を行っている様子）を美容鍼灸タブに追加済み（`.menu-with-image` / `.menu-side-image-wide` パターン）
- [x] `.menu-grid` を3列グリッドから縦1列に変更（全メニューブロック共通）。他の施術メニュー（神経整体・水素吸引・鍼灸施術）にも今後画像を追加していく想定
- [x] 水素吸引の施術風景写真（`images/suiso-kyuin.jpg`、水素吸入器MERUSA1000を使用している様子・実際の院内での撮影）を水素吸引タブに追加済み。横長画像のためトリミングせず全体表示（`.menu-side-image-wide`、aspect-ratio 5:4、object-fit: contain）
- [x] 神経整体の施術風景写真（`images/seitai-sejutsu.jpg`、院長が施術している様子）を神経整体タブに追加済み（`.menu-with-image` / `.menu-side-image-wide` パターン）
- [x] 鍼灸施術タブに施術風景写真（`images/shinkyu-sejutsu.jpg`、背中にはり治療を行っている様子）を追加済み（同パターン）
- [x] 「当院について」の写真を院内スペース（`shisetsu-naikan.jpg`）から問診風景（`images/about-mondou.jpg`）に差し替え済み
- [x] ヘッダーロゴを画像化（`images/logo.png`）。従来のテキストロゴは`.logo-text`として画像の右に併記
- [x] 「神経整体とは」セクションに神経整体T-Groupのエビデンス（2021日本疲労学会学術大会発表・自律神経機能への効果）を追加済み（`.seitai-evidence`）。発表資料原本（`images/evidence.jpg`）をそのまま埋め込み表示
- [x] 院内の施術スペース写真（`images/shisetsu-naikan.jpg`）を`.about-image`に追加済み
- [x] 「施術の流れ」セクション（`#flow`）を新規追加済み（2026-09-08）。問診・検査・施術・次回の予約の4ステップを写真付きカードで表示。ナビゲーションに「施術の流れ」を追加、`#shinkei-seitai`と`#concerns`の間に配置
- [ ] 加工前の元写真（PNG、`~/Downloads/`にランダムなGUIDファイル名で保存）が未削除。不要であれば削除可
